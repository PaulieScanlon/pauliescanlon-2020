---
navigationLabel: home
---

import { format } from "date-fns"
import { mix } from "@theme-ui/color"
import { ProfileInfo } from "../components/ProfileInfo"

<Alert variant="error">I'm currently on contract 😊</Alert>
<Divider />
<Divider />

<ProfileInfo />
<Divider />
<Divider />

<Button
  as="a"
  variant="success"
  href="https://docs.google.com/document/d/17zH4XZAOoIicQtAPraxPwy48UZrogchASAeQjXjlJdM/edit?usp=sharing"
  target="_blank"
  sx={{
    mr: 2,
  }}
>
  View CV/Resume
</Button>

<Link
  href="https://drive.google.com/file/d/1OH6VR_rqASnqcW6qzdSkvkiLH7SWkesO/view?usp=sharing"
  target="_blank"
  title="View CV"
>
  .pdf version
</Link>
<Divider />
<Divider />

## intro

Well hey there! 👋 I'm Paul and a long long time ago I was a designer but after a few years of pixel pushing I realised code is just better! I’ve held various positions in and around advertising and web development in both permanent and contract positions since 2005, and I once co-founded and ran an award winning digital production agency called Super natural based in London.

<Divider />

## more info

In recent months I've developed an interest in technical writing and have started to document my workings and findings on my [blog](http://www.paulie.dev/)

<Button
  as="a"
  variant="secondary"
  href="http://www.paulie.dev/"
  target="_blank"
>
  Visit Blog
</Button>

<Divider />
<Divider />

<Flex sx={{ flexWrap: "wrap" }}>

  <Box
    sx={{
      width: ["100%", "100%", "55%"],
      mb: 3,
      pr: [0,0, 4],
    }}
  >

## contact

<Heading variant="styles.h3" sx={{ mb: 0 }}>
  Email
</Heading>
<Link href="mailto:pauliescanlon@gmail.com" target="_blank" variant="styles.a">
  pauliescanlon@gmail.com
</Link>
<Divider />

<Heading variant="styles.h3" sx={{ mb: 0 }}>
  Twitter
</Heading>
<Link
  href="https://twitter.com/PaulieScanlon/"
  target="_blank"
  variant="styles.a"
>
  https://twitter.com/PaulieScanlon/
</Link>
<Divider />

<Heading variant="styles.h3" sx={{ mb: 0 }}>
  GitHub
</Heading>
<Link
  href="https://github.com/PaulieScanlon/"
  target="_blank"
  variant="styles.a"
>
  https://github.com/PaulieScanlon/
</Link>
<Divider />

<Heading variant="styles.h3" sx={{ mb: 0 }}>
  LinkedIn
</Heading>
<Link
  href="https://www.linkedin.com/in/PaulieScanlon/"
  target="_blank"
  variant="styles.a"
>
  https://linkedin.com/in/PaulieScanlon/
</Link>
<Divider />

  </Box>

  <Box
    sx={{
      width: ["100%", "100%", "45%"],
      mb: 3
    }}
  >

## tech-stack

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("muted", "primary", `${index / tags.length}`),
                borderColor: mix("muted", "primary", `${index / tags.length}`),
              }}
            >
              {name}
            </Badge>
          )
        })}
    </Box>
  )}
</SourceTags>

</Box>

</Flex>

<Divider />

## recent contracts

Paid commercial work

<SourceList filter="portfolio">
  {(projects) => (
    <Grid
      sx={{
        gridTemplateColumns: ["1fr", "1fr", "1fr 1fr"],
      }}
    >
      {projects
        .sort(
          (a, b) =>
            new Date(b.node.frontmatter.date) -
            new Date(a.node.frontmatter.date)
        )
        .map((edge, index) => {
          const {
            frontmatter: { date, dateModified, author, title, tags },
            fields: { slug },
            excerpt,
          } = edge.node
          return (
            <Box
              key={index}
              sx={{
                display: "flex",
                flex: "1 1 auto",
                flexDirection: "column",
              }}
            >
              <Card
                sx={{
                  display: "flex",
                  flex: "1 1 auto",
                  flexDirection: "column",
                  minHeight: "1px",
                  p: 3,
                }}
              >
                <Box
                  sx={{
                    display: "flex",
                    flex: "1 1 auto",
                    flexDirection: "column",
                  }}
                >
                  <Flex>
                    <Text variant="styles.small">
                      {format(new Date(date), "d-MMM-u")}
                    </Text>
                    <Text sx={{ mx: 1 }} variant="styles.small">
                      /
                    </Text>
                    {dateModified && (
                      <Text variant="styles.small">
                        {format(new Date(dateModified), "d-MMM-u")}
                      </Text>
                    )}
                    {author && <Text variant="styles.small">{author}</Text>}
                  </Flex>
                  <Heading variant="styles.h4" sx={{ color: "text", mt: 1 }}>
                    {title}
                  </Heading>
                  <Text
                    sx={{
                      fontSize: 0,
                      color: "text",
                      mb: 3,
                      wordBreak: "break-word",
                    }}
                  >
                    {excerpt}
                  </Text>
                </Box>
                <Box>
                  {tags.map((tag, index) => {
                    return (
                      <Badge
                        key={index}
                        variant="primary"
                        sx={{
                          mr: 2,
                          mb: 2,
                          color: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                          borderColor: mix(
                            "muted",
                            "primary",
                            `${index / tags.length}`
                          ),
                        }}
                      >
                        {tag}
                      </Badge>
                    )
                  })}
                </Box>
              </Card>
            </Box>
          )
        })}
    </Grid>
  )}
</SourceList>

<Divider />
<Divider />

## open source

Open source projects I'm actively maintaining 🕺

<SourceList filter="opensource">
  {(projects) => (
    <Grid
      sx={{
        gridTemplateColumns: ["1fr", "1fr", "1fr 1fr"],
      }}
    >
      {projects
        .sort(
          (a, b) =>
            new Date(b.node.frontmatter.date) -
            new Date(a.node.frontmatter.date)
        )
        .map((edge, index) => {
          const {
            frontmatter: { date, title, tags, author },
            fields: { slug },
            excerpt,
          } = edge.node
          return (
            <Box
              key={index}
              sx={{
                display: "flex",
                flex: "1 1 auto",
                flexDirection: "column",
              }}
            >
              <Card
                sx={{
                  display: "flex",
                  flex: "1 1 auto",
                  flexDirection: "column",
                  minHeight: "1px",
                  p: 3,
                }}
              >
                <Box
                  sx={{
                    display: "flex",
                    flex: "1 1 auto",
                    flexDirection: "column",
                  }}
                >
                  <Text variant="styles.small">
                    {format(new Date(date), "d-MMM-u")}
                  </Text>
                  <Heading variant="styles.h4" sx={{ color: "text", mt: 1 }}>
                    {title}
                  </Heading>
                  <Text
                    sx={{
                      fontSize: 0,
                      color: "text",
                      mb: 3,
                      wordBreak: "break-word",
                    }}
                  >
                    {excerpt}
                  </Text>
                  <Link
                    sx={{ fontSize: "14px", mb: 3 }}
                    href={author}
                    target="_blank"
                  >
                    View on GitHub
                  </Link>
                </Box>
                <Box>
                  {tags.map((tag, index) => {
                    return (
                      <Badge
                        key={index}
                        variant="primary"
                        sx={{
                          mr: 2,
                          mb: 2,
                          color: mix(
                            "muted",
                            "secondary",
                            `${index / tags.length}`
                          ),
                          borderColor: mix(
                            "muted",
                            "secondary",
                            `${index / tags.length}`
                          ),
                        }}
                      >
                        {tag}
                      </Badge>
                    )
                  })}
                </Box>
              </Card>
            </Box>
          )
        })}
    </Grid>
  )}
</SourceList>

<Divider />
