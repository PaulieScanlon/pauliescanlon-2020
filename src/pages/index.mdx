---
navigationLabel: home
---

import { format } from 'date-fns'
import { mix } from '@theme-ui/color'

<Alert variant='success'>
  Alert... I'm looking for remote contract React/Gatsby work.
</Alert>
<Divider />

# [paul-scanlon](#paul-scanlon)

I'm a React UI Developer / UX Engineer (contract) based in Worthing / UK.

React, Gatsby.js, JavaScript, TypeScript/Flow, StyledComponents, Theme UI, Storybook, TDD (Jest/Enzyme) and a tiny bit of Node.js

<Button as='a' variant='success' href='https://docs.google.com/document/d/10Z8w-099fRjVB1zA1Y9F3zVWtW1RfIxnOTIyrYn0HcY/edit' target='_blank'>View CV</Button>{" "}
<Link href='https://docs.google.com/document/d/10Z8w-099fRjVB1zA1Y9F3zVWtW1RfIxnOTIyrYn0HcY/edit' target='_blank' title='View CV'>docs.google.com</Link>
<Divider />
<Divider />
<Divider />

## [intro](#intro)

A long time ago I was a designer, and then after a few years I realised code is just better. I’ve held various positions in and around advertising and web development in both permanent and contract positions since 2005.


You can find out more about me on my [blog](http://www.paulie.dev/) and read my cv/resume [here](https://docs.google.com/document/d/10Z8w-099fRjVB1zA1Y9F3zVWtW1RfIxnOTIyrYn0HcY/edit)

<Divider />

<Flex sx={{ flexWrap: "wrap" }}>

  <Box
    sx={{
      width: ["100%", "100%", "55%"],
      mb: 3,
      pr: [0,0, 4],
    }}
  >         

  ## [contact](#contact)

  <Heading variant='styles.h3' sx={{mb: 0}}>Email</Heading>
  <Link href='mailto:pauliescanlon@gmail.com' target='_blank' variant='styles.a'>pauliescanlon@gmail.com</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0,}}>Twitter</Heading>
  <Link href='https://twitter.com/PaulieScanlon/' target='_blank' variant='styles.a'>https://twitter.com/PaulieScanlon/</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0}}>GitHub</Heading>
  <Link href='https://github.com/PaulieScanlon/' target='_blank' variant='styles.a'>https://github.com/PaulieScanlon/</Link>
  <Divider />

  <Heading variant='styles.h3' sx={{mb: 0}}>LinkedIn</Heading>
  <Link href='https://www.linkedin.com/in/PaulieScanlon/' target='_blank' variant='styles.a'>https://linkedin.com/in/PaulieScanlon/</Link>
  <Divider />

  </Box>

  <Box
    sx={{
      width: ["100%", "100%", "45%"],
      mb: 3
    }}
  >
 

  ## [tech-stack](#tech-stack)

  <TagList>
  {tags => (
      <Box>
        {tags
          .sort((a, b) => b.percent - a.percent)
          .map((tag, index) => {
            const { name, count, percent } = tag
            return (
              <Badge
                key={index}
                variant="primary"
                sx={{
                  mr: 2,
                  mb: 2,
                  color: mix("muted", "primary", `${index / tags.length}`),
                  borderColor: mix(
                    "muted",
                    "primary",
                    `${index / tags.length}`
                  ),
                }}
              >
                {name}
              </Badge>
            )
          }
        )}
      </Box>
    )}
  </TagList>

  </Box>

</Flex>

<Divider />

## [recent contracts](#recent-contracts)

Paid commercial work... all under NDA and on lockdown

<SourceList filter="portfolio">
  {projects => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {projects
      .sort((a, b) => new Date(b.node.frontmatter.date) - new Date(a.node.frontmatter.date))
      .map((edge, index) => {
        const {
          frontmatter: { date, title, tags },
          fields: { slug },
          excerpt
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column', mb: 3, width: ["100%", "50%"] }}
          >
            <Card
              sx={{
                display: 'flex', flex: '1 1 auto', flexDirection: 'column',
                minHeight: '1px',
                ml: 2, 
                mr: 2,
                p: 3,
              }}
            > 
              <Box sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column' }}>
                <Text variant='styles.small'>{format(new Date(date), 'd-MMM-u')}</Text>
                <Heading variant="styles.h4" sx={{color: 'text'}}>{title}</Heading>
                <Text sx={{fontSize:0, color: 'text', mb: 3, wordBreak: 'break-word'}}>{excerpt}</Text>
              </Box>
              <Box>
                {
                  tags.map((tag, index) => {
                    return (
                   <Badge 
                    key={index} 
                    variant='primary' 
                    sx={{
                      mr: 2, 
                      mb: 2,
                      color: mix('muted', 'primary', `${index / tags.length}`),
                      borderColor: mix('muted', 'primary', `${index / tags.length}`)}}>
                      {tag}
                    </Badge>
                    )
                  })
                }
              </Box>
            </Card>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>

<Divider />
<Divider />

## [opensource](#open-source)

Opensource contributions and projects i'm actively maintaining

<SourceList filter="opensource">
  {projects => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {projects
      .sort((a, b) => new Date(b.node.frontmatter.date) - new Date(a.node.frontmatter.date))
      .map((edge, index) => {
        const {
          frontmatter: { date, title, tags, author },
          fields: { slug },
          excerpt
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column', mb: 3, width: ["100%", "50%"] }}
          >
            <Card
              sx={{
                display: 'flex', flex: '1 1 auto', flexDirection: 'column',
                minHeight: '1px',
                ml: 2, 
                mr: 2,
                p: 3,
              }}
            > 
              <Box sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column' }}>
                <Text variant='styles.small'>{format(new Date(date), 'd-MMM-u')}</Text>
                <Heading variant="styles.h4" sx={{color: 'text'}}>{title}</Heading>
                <Text sx={{fontSize:0, color: 'text', mb: 3, wordBreak: 'break-word'}}>{excerpt}</Text>
                <Link sx={{fontSize:0, mb: 3}}href={author} target='_blank'>View on GitHub</Link>
              </Box>
              <Box>
                {
                  tags.map((tag, index) => {
                    return (
                   <Badge 
                    key={index} 
                    variant='primary' 
                    sx={{
                      mr: 2, 
                      mb: 2,
                      color: mix('muted', 'secondary', `${index / tags.length}`),
                      borderColor: mix('muted', 'secondary', `${index / tags.length}`),}}>
                      {tag}
                    </Badge>
                    )
                  })
                }
              </Box>
            </Card>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>

<Divider />
<Divider />

## [side hussle](#side-hussle)

React applications i've built in my spare time, because i love it

<SourceList filter="sidehussle">
  {projects => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {projects
      .sort((a, b) => new Date(b.node.frontmatter.date) - new Date(a.node.frontmatter.date))
      .map((edge, index) => {
        const {
          frontmatter: { date, title, tags, author },
          fields: { slug },
          excerpt
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column', mb: 3, width: ["100%", "50%"] }}
          >
            <Card
              sx={{
                display: 'flex', flex: '1 1 auto', flexDirection: 'column',
                minHeight: '1px',
                ml: 2, 
                mr: 2,
                p: 3,
              }}
            > 
              <Box sx={{ display: 'flex', flex: '1 1 auto', flexDirection: 'column' }}>
                <Text variant='styles.small'>{format(new Date(date), 'd-MMM-u')}</Text>
                <Heading variant="styles.h4" sx={{color: 'text'}}>{title}</Heading>
                <Text sx={{fontSize:0, color: 'text', mb: 3, wordBreak: 'break-word'}}>{excerpt}</Text>
                <Link sx={{fontSize:0, mb: 3}}href={author} target='_blank'>View project</Link>
              </Box>
              <Box>
                {
                  tags.map((tag, index) => {
                    return (
                   <Badge 
                    key={index} 
                    variant='primary' 
                    sx={{
                      mr: 2, 
                      mb: 2,
                      color: mix('muted', 'primary', `${index / tags.length}`),
                      borderColor: mix('muted', 'primary', `${index / tags.length}`),}}>
                      {tag}
                    </Badge>
                    )
                  })
                }
              </Box>
            </Card>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>